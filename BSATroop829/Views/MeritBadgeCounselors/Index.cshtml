@*@model IEnumerable<BSATroop829.Models.MeritBadgeCounselorsViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Local Merit Badge Counselors</h1>

<p>
    <a asp-action="Create" class="btn btn-secondary">
        <i class="bi bi-plus-circle-fill"></i>Create New
    </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MeritBadgeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CounselorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CounselorPhoneNumber)
            </th>
            @if (User.IsInRole("Admin") || User.IsInRole("Committee Member") || User.IsInRole("ScoutMaster") || User.IsInRole("AsstScoutMaster"))
            {
                <th></th>
            }

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MeritBadgeName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CounselorName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CounselorPhoneNumber)
                </td>
                @if (User.IsInRole("Admin") || User.IsInRole("Committee Member") || User.IsInRole("ScoutMaster") || User.IsInRole("AsstScoutMaster"))
                {

                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-secondary">
                            <i class="bi bi-pencil"></i>Edit
                        </a> |
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">
                            <i class="bi bi-card-list"></i>Details
                        </a> |
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">
                                <i class="bi bi-trash"></i>Delete</a>
                    </td>
                }
            </tr>
        }
    </tbody>
</table>
*@

<iframe width="100%" height="1000" src="https://docs.google.com/spreadsheets/d/e/2PACX-1vSe8ihUVA5xC7hTd4pjqXcvexh-RnUl3F63zBuuihpXsgszmn9vwbvY0RbytZ9mB-3Uspiqog7Kv4zo/pubhtml?widget=true&amp;headers=false"></iframe>